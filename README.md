# fastapi_learning
# FastAPI Learning Project 🚀

This project is created to learn FastAPI step by step from beginner to intermediate.

## Features
- Basic routes (GET)
- Path & Query parameters
- POST requests with Pydantic
- Response models
- CRUD operations (In-memory)

## How to Run
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
